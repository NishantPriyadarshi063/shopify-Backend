# Backend only. Run from shopify-Backend: docker compose up -d --build
# Uses .env for DB_* (e.g. DB_HOST=seleric.cloud). No overrides – same DB for local and deployed.

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    environment:
      PORT: 5300
    ports:
      - "5300:5300"
